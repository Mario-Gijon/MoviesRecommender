# Data

This folder is reserved for local catalog assets and derived SQLite data used by the recommender.

Subdirectories under `raw/` and `processed/` are used for offline MovieLens pipeline work.

Real TMDB, MovieLens, and offline catalog files will be expanded later.
